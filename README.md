# Hunter ChatGPT MCP

## Launch MCP Server

To launch the MCP server:

```bash
npx wrangler dev
```

This will start the MCP server locally.

## Launch MCP Inspector

To launch the MCP inspector:

```bash
npx @modelcontextprotocol/inspector
```

This will start the MCP inspector tool for debugging and testing.